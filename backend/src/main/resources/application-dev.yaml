spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/minhacasa
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

info: ===== development =====

#path for save files
file.upload-image-profile-dir: "C:/app/images-profile"
file.upload-image-immobile-dir: "C:/app/files-immobile/"

#path for mount files
base-url: "http://localhost:8080"
base-url-user-files-api: "/api/files/download/profile/"
base-url-immobile-files-api: "/api/files/download/immobile/"
